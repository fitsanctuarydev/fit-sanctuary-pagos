{
  "name": "fit-sanctuary-api",
  "version": "1.0.0",
  "description": "Backend pagos",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && cd client && npm install --include=dev && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mercadopago": "^2.4.2",
    "stripe": "^14.15.0",
    "axios": "^1.7.0",
    "nodemailer": "^6.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
